Nm

Act as a Senior Full-Stack Game Developer and Architect. Build a 100% complete, functional, and secure web-based Multiplayer 2D Sandbox Mining & Passive Earning Game optimized for mobile-first screens. The system must use Node.js (Express) for the backend, vanilla JavaScript with HTML5 Canvas for the frontend, and Replit's built-in PostgreSQL database for data persistence.

Do not use placeholders or write partial "TODO" code. Implement every single file and system completely so that it runs out-of-the-box on Replit.

---

### CORE APP COMPONENTS TO BUILD:

#### 1. Database Schema (PostgreSQL)
Design and automatically initialize a database structure with the following tables:
- `users`: id, username, password_hash, created_at.
- `wallets`: user_id, gems (integer), world_locks (integer), diamond_locks (integer), real_balance (numeric 12,6).
- `miners`: user_id, level (default 1), current_balance (numeric 12,6), temperature (numeric, max 100), efficiency (numeric), last_maintenance_at.
- `worlds`: id, name (unique), owner_id, block_data (JSON matrix representing a grid of customizable blocks).

#### 2. The 3-Hour Dynamic Revenue Pool System
- Implement a backend cron job or time-check algorithm that executes every 3 hours.
- It must track the "Total Simulated Adsterra Earnings" generated by user actions (mocking the API data stream).
- It takes a 30% "House Cut" for the owner, then distributes the remaining 70% pool to active users using this exact formula:
  User Payout = (User Points in Window / Total Points in Window) * Available Payout Pool.
- Automatically clear the 3-hour temporary point balances to 0 upon completion, converting their shares into real withdrawable cash balances.

#### 3. Active Gameplay Loop (Growtopia Style)
- Create a mobile-responsive HTML5 Canvas 2D grid world.
- Players can move their character (Left, Right, Jump) using on-screen touch controls or arrow keys.
- Players can punch grid blocks (Dirt, Rock, Lava, gold, diamond, iron) to clear space and randomly harvest Gems or Seeds. The seeds can have oak tree seeds and can have pickaze remember minecraft wood stone iron maybe gold is for mining center
- Implement an inventory system allowing users to buy, sell, and trade "World Locks" and "Diamond Locks" with other players or a central item store.

#### 4. The Passive "Data Center Miner" Loop
- Create a dashboard tab where users construct, upgrade, and maintain a "Data Center Crypto Miner." They have to craft 
- The miner generates a live balance stream starting at $0.000001 per second, scaling upwards based on miner level upgrades. (Heres the secret its 1 sats perday and it can be upgraded into 30 sats perday and it can be upgraded more this is just the beginnr level)
- Real-Time Tick Frontend: The frontend must update this balance smoothly every 100 milliseconds using local JavaScript calculations based on timestamps to maximize visual dopamine without overloading server requests.
- Overheating Mechanic: The miner's temperature climbs dynamically over a 24-hour window. If it hits 100°C, production halts. The user must manually click "Flush Water Cooling" or "Apply Thermal Paste" once a day to reset the thermal gauge. And they need solar panel for electricity or manual startup or thermal energy they can buy solar with gens

Crafting and putting block together mechanics: you see this is inspired by growtopia or minecraft red stone they can craft thermal phase and water bucket

#### 5. Monetizationf & Arbitrage Integration
- Design UI containers/buttons where users can click to "Overcharge Mining Drill" or "Instantly Cool Down Data Center Server Room."
- These buttons must securely trigger external routing (simulating an Adsterra Smartlink/Popunder opening in a new tab) and start a 15-second visual countdown on the game dashboard. They need to be 
- Only after the countdown finishes can the client ping the backend to verify the action and award the speed boost or maintenance fix.

Security Rule: Never trust the frontend alone to issue the actual reward. When the 15 seconds expire on the client side, send a timestamped request to your backend server. The server should check if at least 15 seconds have truly elapsed since that specific user session requested the task before unlocking the payout database.

#### 6. Anti-Cheat & Security Layer
- Never trust the client. All block-breaking events, gem drops, and point updates must be checked and executed on the Node.js backend.
- Implement a "Fatigue System" / "Grind Braker": Track total user requests inside the 3-hour window. First 50 actions yield 100% points, actions 51-100 yield 50% points, and actions 101+ drop to 10% points to completely isolate and neutralize clicker-bots.
- Implement a hidden "Wizard Challenge" verification (in-game modal pop-up) if a user executes actions too rapidly over a rolling 2-minute window.
And maybe they have mission break specific number of blocks and watch ads or chat
---

### UI/UX DESIGN REQUIREMENTS:
- Mobile-First responsive design layout with an immersive, high-tech arcade style.
- Use a dark theme with neon-green values for rolling cash numbers, fire-red meters for overheating alerts, and crisp retro pixel borders.
- Include a rolling leaderboard showing live player transactions, top active miners, and current live market valuations for World Locks based on daily ad yield metrics.

Build this systematically. Start by setting up the database and backend foundations, followed by the core game mechanics, the passive engine loop, and finally the polished frontend interface.